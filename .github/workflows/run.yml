name: Routine Music Handler
run-name: Routine Music Handler (${{ github.event_name }})

on:
  workflow_dispatch:
  repository_dispatch:
    types: [GoogleAppScript_RoutineMusicHandler]

concurrency:
  group: routine-music-handler
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      # Environment configs — can be overridden via GitHub Secrets or workflow dispatch inputs
      GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
      LOGGING_LEVEL: ${{ vars.LOGGING_LEVEL }}

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install Poetry
        run: |
          pip install poetry

      - name: 🧰 Install dependencies
        run: |
          poetry install
          poetry add git+https://github.com/kaianolevine/kaiano-common-utils.git

      - name: 🏃 Run Routine Music Handler
        run: poetry run python src/routine_music_handler/main.py

      - name: 🌐 Checkout website repo
        uses: actions/checkout@v4
        with:
          repository: ${{ 'kaianolevine/website-sandbox' }}
          ref: ${{ 'main' }}
          token: ${{ secrets.WEBSITE_REPO_TOKEN }}
          path: website

      - name: 📄 Copy JSON snapshot into website repo
        run: |
          mkdir -p website/src/v1/routine-music
          cp -f v1/routine-music/submitted_music.json website/src/v1/routine-music/submitted_music.json

      - name: ✅ Commit and push website repo (if changed)
        run: |
          cd website
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/v1/routine-music/submitted_music.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Update routine music JSON"
          git push